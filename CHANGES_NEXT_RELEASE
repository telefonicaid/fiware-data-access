- Add: postgres db connection pool and env vars (`FDA_PG_POOL_MAX`, `FDA_PG_POOL_IDLE_TIMEOUT_MS`, `FDA_PG_POOL_CONN_TIMEOUT_MS`) (#28)
- Add: response format selection in query and doQuery using outputType, with csv and xls support (#20)
- Fix: value of default params are checked when creating DA (#107)
- Breaking Change: query operation is now REST-like using `GET /{scope}/fdas/{fdaId}/das/{daId}/data` instead of `/query` (#62)
- Add: scope-based data access segmentation (`public` / `private`) with FDA scope validation, including CDA `doQuery` compatibility path handling (#21)
